using UnityEngine;

public class SOManager : MonoBehaviour
{
    MatchingStatus mStatus;
    int plasSo = 0;
    void Start()
    {
        GetComponent<MatchingStatus>();
    }

    // Update is called once per frame
    void Update()
    {
        //if(mStatus.IsPlz) 
    }
}
